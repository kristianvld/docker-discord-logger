{
  "name": "docker-discord-logger",
  "version": "1.0.0",
  "description": "A simple docker-to-discord logger.",
  "keywords": [
    "docker",
    "discord",
    "logging"
  ],
  "main": "dist/main.js",
  "type": "module",
  "types": "./dist/main.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "tsc && node dist/main.js",
    "start": "node dist/main.js",
    "format": "prettier --config .prettierrc 'src/**/*.ts' --write"
  },
  "author": "kristianvld",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@types/dockerode": "^3.3.11",
    "discord.js": "^14.6.0",
    "dockerode": "^3.3.4",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.2.1",
    "strip-ansi": "^7.0.1"
  }
}
